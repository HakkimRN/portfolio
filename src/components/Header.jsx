import React from "react";

const Header = () => {
  return (
    <div className="">
      <button>Projects</button>
      <button>About</button>
      <button>Resume</button>
    </div>
  );
};

export default Header;
